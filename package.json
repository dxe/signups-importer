{
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "push": "vitest run && clasp push"
  },
  "dependencies": {
    "@types/google-apps-script": "^1.0.41"
  },
  "devDependencies": {
    "@google/clasp": "^2.5.0",
    "esbuild": "^0.28.0",
    "vitest": "^4.1.6"
  }
}
